export default (params) => {
 return(
    <>
    <h1>Private Folder</h1>
    </>
 ) 
}
// to make the folder private write underscore(_) before folder name , in this case private folder means that folder which not included during routing
import HeroSection from "./components/HeroSection";

 
export default function page() {
  return (
    <>
      <HeroSection title={"Let's Watch Movies Together"} imageUrl={"/home.svg"}/>
    </>
  )
}
// Shape divider a website to create wave in an image
// To connect with api make folder in app named api create inner folder in api folder with respectible name in that folder create a file name route.js(mendatory)